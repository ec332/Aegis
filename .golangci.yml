run:
  timeout: 3m
  tests: true
linters:
  enable:
    - govet
    - staticcheck
    - gofmt
    - revive
    - ineffassign
    - unused
    - gosimple
issues:
  exclude-use-default: false
  max-same-issues: 0
  max-issues-per-linter: 0
